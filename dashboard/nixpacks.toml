[phases.build]
cmds = [
    "pip install -r requirements.txt"
]

[phases.setup]
nixPkgs = ["python311", "gcc"]

[start]
cmd = "gunicorn -b 0.0.0.0:$PORT -w 2 --timeout 60 wsgi:application"

[variables]
PORT = "8000"
PYTHONUNBUFFERED = "1"
DASH_DEBUG = "false"